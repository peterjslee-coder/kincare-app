# New Feedback — March 19, 2026

> Auto-generated by scheduled feedback-loop task. Review and merge into TASKS.md during next dev session.

## Access Issue — Could Not Fetch Feedback

The scheduled feedback loop was unable to retrieve feedback from the production API. All admin endpoints returned **401 — "Admin TOTP code required (x-admin-totp header)"**.

**What was tried:**

1. `GET /api/admin/feedback/triage` with `x-admin-api-key` header — 401, requires TOTP
2. `GET /api/feedback` with `x-admin-api-key` header — 401, requires TOTP
3. Direct `curl` from VM — blocked by outbound proxy (403)
4. Browser-based `fetch()` from yourinplace.com — API reachable (waitlist endpoint confirmed OK), but all admin/feedback endpoints require TOTP on top of API key

**Root cause:** The production server requires a TOTP code (`x-admin-totp` header) in addition to the API key for all admin endpoints. The scheduled task cannot generate TOTP codes without the 2FA secret.

## Recommended Fix (pick one)

1. **Exempt feedback triage from TOTP when using API key.** The `ADMIN_API_KEY` is already a strong secret — requiring TOTP blocks automation. Allow API-key-only access for read-only endpoints like `/api/admin/feedback/triage`.
2. **Add `ADMIN_TOTP_SECRET` to `.env`** so the task can generate codes at runtime via `otplib`.
3. **Create a dedicated read-only feedback endpoint** that requires only the API key.

Option 1 is cleanest — preserves TOTP for destructive admin actions while enabling automated read-only feedback collection.

## New Items (not yet in TASKS.md)

*Unable to fetch — see access issue above.*

## Status Update on Existing Items

*Unable to cross-reference — no feedback data retrieved.*

## Feedback Stats

- Total feedback items: **Unknown** (access blocked)
- New since last check (Mar 18): **Unknown**
- By category: **Unknown**

---

**Previous run (Mar 18):** 50 total items, 1 new (caregiver referral bonus feature request — added to TASKS.md as P2). All others reviewed/planned/done.

**Next steps for Pete:** Fix the TOTP requirement for API-key-authenticated feedback endpoints, then re-run the feedback loop.
